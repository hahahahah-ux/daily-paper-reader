---
title: "WorldAgen: Unified State-Action Prediction with Test-Time World Model Training"
title_zh: WorldAgen：具有测试时世界模型训练的统一状态-动作预测
authors: "Chi Wan, Kangrui Wang, Yuan Si, Pingyue Zhang, Manling Li"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08162v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 结合VLA、世界建模与测试时自适应的统一状态-动作预测
tldr: 视觉-语言-动作模型在动态变化的新环境中泛化不足，现有世界模型方法依赖静态预训练，缺乏部署时的主动适应。WorldAgen 提出统一框架，用共享 Transformer 主干与混合单向注意力掩码，联合学习世界模型和动作预测，并在测试时探索采样并轻量更新微调世界模型。在 CALVIN 和 LIBERO 上，基线性能与当前最优方法相当甚至更优；仅用少量样本做测试时训练即可超越现有最优模型，表明推理时适应世界模型能显著提升动作预测与环境理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.08162v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 600, \"height\": 512}]"
motivation: 现有VLA模型结合世界模型与动作预测，但仅依赖静态数据预训练，部署时无法主动适应新环境，导致在未见场景中泛化失败。
method: WorldAgen 用共享 Transformer 主干与混合单向注意力掩码，联合学习世界模型与动作预测，测试时采样探索并微调世界模型。
result: 在 CALVIN 和 LIBERO 上，基线性能与当前最优方法相当甚至更优；仅用少量样本做测试时训练即可超越现有最优模型。
conclusion: 表明推理时适应世界模型能显著提升动作预测与环境理解，为 VLA 部署时主动适应提供有效方案。
---

## 摘要
视觉-语言-动作（VLA）模型如何适应世界动态发生变化的新环境？尽管近期研究将世界建模与动作预测相结合以提升 VLA 性能，但现有方法大多依赖在静态数据集上进行预训练，缺乏在部署时主动适应的机制。因此，当部署到具有新颖物体配置或动态的未见场景中时，这些模型往往难以泛化。我们提出 WorldAgen，一个统一框架，能够联合学习世界建模与动作预测，同时支持测试时训练（TTT）以适应新环境。WorldAgen 采用共享的 Transformer 主干网络和两个头：（1）世界模型头，根据过去的状态-动作轨迹预测未来状态；（2）智能体模型头，以任务指令为条件预测动作。我们设计了一种混合单向注意力掩码（Mixed Unidirectional Attention Mask）来分离这两个模型。在测试时，WorldAgen 采样探索性动作，收集真实状态转移，并执行轻量级 TTT 更新以精炼其世界模型。这种适应提升了模型对环境的理解，并带来更准确的动作预测。在 CALVIN 和 LIBERO 基准上的实验表明，我们的基线模型取得了与当前最先进方法相当、在某些情况下更优的性能。此外，在少量样本上进行 TTT 时，我们的方法超越了现有的最先进模型，凸显了在推理时适应世界模型的有效性。

## Abstract
How can vision-language-action (VLA) models adapt to new environments where world dynamics shift? While recent research has combined world modeling and action prediction to improve VLA performance, existing methods largely rely on pretraining on static datasets, without mechanisms for active adaptation at deployment time. As a result, these models often fail to generalize when deployed in unseen scenarios with novel object configurations or dynamics. We present WorldAgen, a unified framework that jointly learns world modeling and action prediction while enabling Test-Time Training (TTT) to adapt to new environments. WorldAgen employs a shared Transformer backbone with two heads: (1) a world model head that predicts future states from past state-action trajectories, and (2) an agent model head that predicts actions conditioned on task instructions. We design a Mixed Unidirectional Attention Mask to separate these two models. During test time, WorldAgen samples exploratory actions, collects ground-truth state transitions, and performs lightweight TTT updates to refine its world model. This adaptation improves the model's understanding of the environment and leads to more accurate action predictions. Experiments on the CALVIN and LIBERO benchmarks demonstrate that our baseline model achieves comparable, and in some cases superior, performance to current state-of-the-art approaches. Moreover, with TTT on a small number of samples, our method surpasses existing state-of-the-art models, highlighting the effectiveness of adapting world models at inference time.

---

## 论文详细总结（自动生成）

# WorldAgen 论文中文总结

## 1. 核心问题与整体含义
- **研究动机**：视觉-语言-动作（VLA）模型已能将语言、视觉与机器人控制结合，近期工作也尝试联合“世界建模”和“动作预测”以提升数据效率与场景理解。
- **核心问题**：现有方法大多依赖静态数据集预训练，部署后缺乏主动适应机制。当新环境出现物体布局、光照、物理属性等分布偏移时，模型对世界动态的内部表征无法更新，导致泛化失败。
- **整体含义**：论文提出 WorldAgen，将世界建模从“被动预训练目标”转变为“测试时主动适应机制”。通过在推理阶段用少量探索数据轻量更新世界模型，间接提升动作预测与长时程操作能力。

## 2. 方法论
### 2.1 总体架构
- WorldAgen 采用**共享 Transformer 主干 + 两个预测头**：
  - **世界模型头**：根据历史状态-动作轨迹预测未来观测/状态，任务无关。
  - **智能体策略头**：根据任务指令、当前观测与状态预测动作。
- 任务建模为 POMDP。轨迹被划分为轨迹单元 \(U_i=(G_i,O_i,S_i,A_i,A_p,O_p)\)，其中 \(G_i\) 为任务指令，\(O_i,S_i,A_i\) 分别为观测、机器人状态、动作块，\(A_p,O_p\) 为占位符。
- 智能体历史为 \(h_a^i=(G_i,O_x,S_x,\hat A_x)_{x=i-k}^{i-1}\)，世界历史为 \(h_w^i=(O_x,S_x,\hat A_x)_{x=i-k}^{i-1}\)。
- 预测形式：
  - 策略头：\(p_a(\hat A_i|G_i,O_i,S_i,h_a^i)\)
  - 世界模型头：\(p_w(\hat O_i|O_i,S_i,\hat A_i,h_w^i)\)
- 训练时采用 teacher forcing，用真实动作 \(A_i\) 替代预测动作 \(\hat A_i\)。

### 2.2 轨迹切分与动作分块
- 支持不同观测块长度 \(n\) 与动作块长度 \(m\)。
- 初始单元 \(U_0\) 聚合前 \(n\) 个时间步；后续单元从连续 \(m\) 步中均匀采样 \(n\) 个观测/状态，并取 \(m\) 个动作组成动作块。
- 目的：减少相邻视觉帧冗余，提高训练与部署效率，同时保留动作时序信息。

### 2.3 混合单向注意力
