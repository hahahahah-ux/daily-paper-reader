---
title: "ActSafeGuard: Differentiable and Training-Aligned Constraint Enforcement for Flow-Matching Policies"
title_zh: ActSafeGuard：面向流匹配策略的可微且与训练对齐的约束执行
authors: "Jianming Ma, Rongjun Jin, Xiaxi Si, Yang Zhang, Yiheng Li, Yue Gao"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11697v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: VLA与世界-动作模型的约束执行
tldr: "背景：VLA/WAM 生成动作可能违反硬物理约束，现有方法要么只优化统计安全无逐步确定性保证，要么仅在推理时修正导致训练执行不一致。方法：提出 ActSafeGuard，可微分、与训练对齐的安全防护层，通过解析射线缩放算子提供边界感知梯度，将硬动作可行性融入策略学习。结果：在 π0.5 与 Fast-WAM 上多任务实验达到 100% 逐步安全率，同时保持甚至提升任务成功率。意义：为安全具身智能部署提供可扩展、低侵入方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.11697v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 1591, \"height\": 1106}, {\"url\": \"assets/figures/arxiv/2609.11697v1/fig-002.webp\", \"caption\": \"\", \"page\": 7, \"index\": 2, \"width\": 1200, \"height\": 220}, {\"url\": \"assets/figures/arxiv/2609.11697v1/fig-003.webp\", \"caption\": \"\", \"page\": 7, \"index\": 3, \"width\": 1200, \"height\": 220}]"
motivation: VLA/WAM 生成的动作可能违反硬物理约束，现有方法缺乏逐步确定性保证或存在训练-推理不一致，难以安全部署。
method: 提出 ActSafeGuard 可微分安全防护层，用解析射线缩放算子提供边界感知梯度，把硬动作可行性融入流匹配策略训练。
result: "在 π0.5 与 Fast-WAM 多任务实验中实现 100% 逐步安全率，且任务成功率保持或提升。"
conclusion: 将安全约束内化到策略训练，为具身智能安全部署提供可扩展、低侵入方案。
---

## 摘要
视觉-语言-动作（VLA）模型与世界-动作模型（WAMs）在通用机器人操作中已展现出强大能力，但它们生成的动作可能违反硬物理约束，因而在部署中不安全或不可行。现有安全方法要么优化统计安全目标而缺乏确定性的逐步保证，要么仅在推理时纠正不安全动作，导致策略训练与执行之间存在不匹配。我们提出 ActSafeGuard，一种面向基于流匹配策略的可微且与训练对齐的安全防护层。ActSafeGuard 将动作硬可行性融入策略学习，而不仅仅将安全性视为推理时的外部组件。通过解析式射线缩放算子设计，ActSafeGuard 使边界感知梯度能够引导模型自然学习约束流形。在多种任务上，基于多个标准基础骨干（$π_{0.5}$ 和 Fast-WAM）的大量实验表明，ActSafeGuard 始终实现 $100\%$ 的每步安全率，同时完全保持甚至提升任务成功率，为安全的具身 AI 部署提供了一种可扩展且最小侵入的解决方案。

## Abstract
Vision-Language-Action (VLA) and World-Action Models (WAMs) have demonstrated strong capabilities in general-purpose robotic manipulation, yet their generated actions may violate hard physical constraints and therefore be unsafe or infeasible for deployment. Existing safety approaches either optimize statistical safety objectives without deterministic per-step guarantees or correct unsafe actions only during inference, creating a mismatch between policy training and execution. We introduce ActSafeGuard, a differentiable and training-aligned safeguard layer for flow-matching based policies. ActSafeGuard integrates hard action feasibility into policy learning, not merely treating safety as an inference-time external component. Through an analytical ray-scaling operator design, ActSafeGuard enables boundary-aware gradients to guide the model to naturally learn constrained manifolds. Extensive experiments on multiple standard foundation backbones ($π_{0.5}$ and Fast-WAM) across various tasks demonstrate that ActSafeGuard consistently achieves a $100\%$ step safety rate while fully preserving or even boosting task success rates, providing a scalable and minimally invasive solution for safe embodied AI deployment.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：VLA 与世界-动作模型（WAMs）在通用机器人操作中表现强，但生成动作可能违反硬物理约束（关节位置限制、速度边界、工作空间限制等），单步不可行就可能导致硬件损坏或危险行为。
- **现有方法的两难**：
  - 训练时约束方法：将安全代价或正则项加入策略优化，通常只提供概率/期望意义的安全，缺乏逐步确定性保证。
  - 推理时安全防护：通过投影、裁剪或优化滤波修正动作，能强制硬约束，但修正机制不参与训练，造成训练—推理不匹配，可能扭曲动作分布并降低任务成功率。
- **核心含义**：论文认为关键问题不只是“缺少安全机制”，而是**硬约束执行与策略学习分离**。ActSafeGuard 试图把硬动作可行性直接内化到流匹配策略的训练与生成过程中，实现训练—推理对齐的安全约束执行。

## 2. 方法论

- **核心思想**：在流匹配动作头输出端插入一个**可微分、无额外可学习参数**的射线缩放算子。它保留预训练速度场给出的更新方向，只在更新会越过约束边界时缩短步长，使每个离散流更新都停留在可行域内。
- **离散流匹配建模**：
  - 将连续流匹配离散为 \(N\) 步，\(\tau_k=k/N\)，插值状态 \(x_k=(1-\tau_k)x_0+\tau_k x_1\)。
  - 目标一步更新为 \(\Delta^\star=(x_1-x_0)/N\)。
  -
