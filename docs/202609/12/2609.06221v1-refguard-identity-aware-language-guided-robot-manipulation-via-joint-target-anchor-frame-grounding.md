---
title: "RefGuard: Identity-Aware Language-Guided Robot Manipulation via Joint Target-Anchor-Frame Grounding"
title_zh: RefGuard：基于目标-锚点-参考系联合定位的身份感知语言引导机器人操作
authors: "Lan Wei, Kangyi Lu, Yongchen Wang, Chenmeng Bi, Qi Chen, Hanlin Niu, Yip Fun Yeung, Dandan Zhang"
date: 2026-09-05
pdf: "https://arxiv.org/pdf/2609.06221v1"
tags: ["query:embodied-ai"]
score: 9.0
evidence: 语言引导机器人操作的VLA定位
tldr: "在语言引导的机器人操作中，杂乱场景里的重复物体、模糊锚点与依赖参考系的方位词，常使机器人在语义相容但并非目标的对象上执行动作，即“身份切换”。RefGuard 通过联合维护目标、锚点与参考系三者的后验，推迟承诺：它从 RGB-D 构建以物体为中心、按参考系条件化的场景图，将参考系无关几何与方向关系分离，再用决策策略选择执行、澄清、重观测或中止。在真实 UF850 机械臂上，模糊压力测试中零身份切换、正确率 90.0%，基线则有 33-46% 切换；3200 回合程序化测试中正确执行率由 56.6% 升至 80.5%，且推迟更少。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.06221v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 3497, \"height\": 1842}, {\"url\": \"assets/figures/arxiv/2609.06221v1/fig-002.webp\", \"caption\": \"\", \"page\": 2, \"index\": 2, \"width\": 4830, \"height\": 1695}, {\"url\": \"assets/figures/arxiv/2609.06221v1/fig-003.webp\", \"caption\": \"\", \"page\": 5, \"index\": 3, \"width\": 3891, \"height\": 1535}, {\"url\": \"assets/figures/arxiv/2609.06221v1/fig-004.webp\", \"caption\": \"\", \"page\": 6, \"index\": 4, \"width\": 3694, \"height\": 1058}, {\"url\": \"assets/figures/arxiv/2609.06221v1/fig-005.webp\", \"caption\": \"\", \"page\": 7, \"index\": 5, \"width\": 5390, \"height\": 916}, {\"url\": \"assets/figures/arxiv/2609.06221v1/fig-006.webp\", \"caption\": \"\", \"page\": 8, \"index\": 6, \"width\": 3400, \"height\": 829}]"
motivation: 语言引导操作中，目标、锚点与参考系三者耦合，过早锁定其一会把残余歧义变成静默且不可逆的错误，即身份切换。
method: 构建参考系条件化的物体中心场景图，分离几何与方向关系，并维持目标-锚点-参考系联合后验，用策略决定执行、澄清、重观测或中止。
result: "真实 UF850 臂上模糊测试零身份切换、正确率 90.0%，基线切换率 33-46%；无歧义场景保持 93.3%，场景变化恢复 86.7%。"
conclusion: "在 3200 回合程序化测试中，正确执行率由 56.6% 提升至 80.5%，推迟率由 43.4% 降至 19.5%，验证了延迟承诺的有效性。"
---

## 摘要
视觉-语言-动作（VLA）模型显著推进了语言引导的机器人操作，然而可靠执行仍然取决于识别指令所指的物理对象。在包含重复物体、模糊锚点或依赖参考系的空间术语的杂乱场景中，机器人可能对语义兼容但并非预期的实例执行几何上有效的动作；我们将这种失败称为身份切换。指代对象由三个耦合的潜在变量共同决定：目标、锚点和参考系，因此在执行前就确定其中任何一个，都会将残余歧义转化为无声且不可逆的错误。我们提出 RefGuard，一种身份感知的定位框架，通过维护所有三个变量的联合后验来延迟承诺。RefGuard 从 RGB-D 观测中构建以对象为中心、以参考系为条件的场景图，将参考系无关的几何与方向关系分离，并通过决策策略对后验进行路由，以执行、澄清、重新观测或中止。在真实 UF850 机械臂上，RefGuard 在任何歧义压力试验中均未记录到身份切换，并在其中 90.0% 的试验中正确执行；而微调 VLA 和基于 LLM（大语言模型）的基线在相同试验中有 33-46% 发生身份切换，同时在无歧义场景中保持 93.3% 的成功率，并在 86.7% 的试验中从定位后场景变化中恢复。在包含 3200 个回合的程序化测试套件上，相较于在目标之前就确定锚点和参考系的消融方法，它将可解指令上的正确执行率从 56.6% 提升至 80.5%，同时延迟更少（19.5% 对 43.4%）。

## Abstract
Vision-language-action (VLA) models have substantially advanced language-guided robot manipulation, yet reliable execution still hinges on identifying which physical object an instruction refers to. In cluttered scenes containing repeated objects, ambiguous anchors, or frame-dependent spatial terms, a robot can execute a geometrically valid action on a semantically compatible but unintended instance; we call this failure an identity switch. The referent is jointly determined by three coupled latent variables: the target, the anchor, and the reference frame, so committing to any one of them before execution turns residual ambiguity into a silent and irreversible error. We propose RefGuard, an identity-aware grounding framework that delays commitment by maintaining a joint posterior over all three variables. RefGuard builds a frame-conditioned object-centric scene graph from RGB-D observations, separating frame-independent geometry from directional relations, and routes the posterior through a decision policy that executes, clarifies, reobserves, or aborts. On a real UF850 arm, RefGuard records no identity switch on any ambiguity-stress trial and executes correctly on 90.0% of them, whereas fine-tuned VLA and LLM (Large Language Model)-based baselines switch identity in 33-46% of the same trials, while retaining 93.3% success on unambiguous scenes and recovering from post-grounding scene changes in 86.7% of trials. On a 3200-episode procedural suite, it raises correct execution on solvable instructions from 56.6% to 80.5% over the ablation that commits to the anchor and frame before the target, while deferring less often (19.5% vs. 43.4%).

---

## 论文详细总结（自动生成）

# RefGuard 论文中文总结

## 1. 核心问题与整体含义

- **研究背景**：VLA（视觉-语言-动作）模型显著推进了语言引导的机器人操作，但可靠执行的关键瓶颈并不在于动作生成本身，而在于"指令到底指哪个物理对象"这一指代定位问题。
- **核心问题——身份切换（Identity Switch）**：在杂乱场景中（存在重复物体、模糊锚点、依赖参考系的方位词），机器人可能对一个**语义上兼容、几何上可抓取、但并非用户意图**的实例执行有效动作。该失败不属于任何单一模块（检测、推理、抓取规划都正确），而是源于定位环节在歧义下过早"硬承诺"。
- **问题的结构性根源**：指代对象由三个**相互耦合的潜在变量**共同决定——目标（target）、锚点（anchor）、参考系（frame）。例如"把罐头右边那个甜椒放进杯子旁的碗里"中，"哪个甜椒/哪个罐头/哪个碗/以谁为参照读'右边'"彼此约束。只要在执行前固定其中任一变量，残余歧义就会被"静默地"转化为不可逆的物理错误。
- **现有方法的不足**：
  - 端到端 VLA（GR00T、π0.5）与 LLM 驱动流水线、VLM 定位器普遍**早期承诺**：先选一个最可能的目标/锚点/参考系，再交给下游规划，或直接把选择隐式折叠进动作预测。
  - 追踪类系统只负责"维持已分配的身份"，不解决"语言最初指的是哪个身份"。
  - 交互求助类方法在"计划之间"做选择，而非在单个动作内部消解目标-锚点-参考系的耦合歧义。
- **整体含义**：论文主张把"身份保持"重构为对三元耦合变量的**联合后验推断**，把承诺推迟到"定位→动作"接口处，使系统具备对自身定位可靠性的觉察，从而区分"有理由的正确执行"与"自信的错误执行"。

## 2. 方法论

### 2.1 核心思想
- **延迟承诺（late commitment）**：不选择单一 top-1 指代，而是维持目标-锚点-参考系的联合后验，直到后验足够集中才执行；否则澄清、重观测或中止。
- **模块化接口**：RefGuard 与学习的 LLM/VLM 定位器互补——后者可提供语言解释、歧义预测或假设分数，RefGuard 负责在物理实例层面保持并验证联合不确定性。

### 2.2 语言解析
- 使用 **GPT-4o** 作为语言解析器，将自然语言指令 $x$ 分解为有序的符号化原语查询序列 $\Pi=(Q_1,\dots,Q_L)$，顺序定位与执行。
- 解析器**只输出符号任务结构，不做实例级定位**（不绑定语言短语到检测实例）。
- 每个原语查询表示为 $Q_\ell=(\alpha_\ell, q_{t,\ell}, \{(q^{(k)}_{a,\ell}, r^{(k)}_\ell)\}_{k=1}^{m_\ell}, R_{0\ell}, c_{F,\ell}, C_{act,\ell})$：
  - $\alpha_\ell$：动作（PICK / PLACE）；$q_{t,\ell}$：目标短语；
  - $(q^{(k)}_{a,\ell}, r^{(k)}_\ell)$：锚点短语及其与目标的关系（支持多锚点）；
  - $R_{0\ell}$：无锚点但依赖参考系的约束（如"我左边的物体"）；
  - $c_{F,\ell}$：参考系线索（机器人视角 / 人视角 / 未指定）；
  - $C_{act,\ell}$：动作约束（可抓取性、可放置性、支撑、容纳等）。
- 线索 $c_{F,\ell}$ 只记录"暗示的参考系"，不解析为具体坐标系，残余参考系歧义留待定位模块处理。

### 2.3 物体中心场景图与候选证据
- 基于 **SAM 3** 融合外置第三人称相机与腕部相机的 RGB-D 观测，得到实例集合 $V=\{o_i\}$。
- 节点 $o_i=(m_i,u_i,x_i,b_i,c_i,h_i,\sigma_i,\nu_i)$ 聚合几何（掩码、像心、基座系 3D 位置、包围盒）、语义（类别/属性证据、抓取与可供性元数据）与不确定性（检测不确定度 $\sigma_i$、可见性 $\nu_i$）。
- **节点身份是当前场景局部的，而非持久追踪 ID**：每次观测后重建图并重新推断身份，从而支持场景变化后的恢复。
- **两层场景图** $G=(V,E_{pos},\{E^f_{dir}\}_{f\in F})$：
  - 参考系无关层 $
